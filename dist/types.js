// muni-dev-cost — shared types. The NORMALIZED schema is the product: every
// jurisdiction's fees, however published (per-LUE, per-meter, per-dwelling,
// per-sqft), is mapped into ONE common shape so an agent can compare apples to
// apples. This file ships in the tarball (the thin client renders these), so it
// carries NO data and NO logic — just the shape.
export {};
